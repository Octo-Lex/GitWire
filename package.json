{
  "name": "gitwire",
  "version": "0.23.1",
  "private": true,
  "description": "AI-powered GitHub account management platform",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "npm --workspace=@gitwire/web run dev",
    "build": "npm run --workspaces build",
    "lint": "eslint packages/*/src/",
    "test": "npm run test:unit",
    "test:unit": "npm --workspace=@gitwire/core test && npm --workspace=@gitwire/rules test && npm --workspace=@gitwire/runtime test && npm --workspace=@gitwire/web run test:unit && npm --workspace=@gitwire/executor-service test && npm --workspace=web-dashboard test",
    "test:integration": "npm --workspace=@gitwire/web run test:integration",
    "test:stress": "npm --workspace=@gitwire/web run test:stress",
    "test:all": "npm --workspace=@gitwire/web run test:all",
    "db:migrate": "node scripts/migrate.js",
    "start": "npm --workspace=@gitwire/web run start",
    "prepare": "husky 2>/dev/null || true",
    "benchmark": "node scripts/benchmark.js",
    "benchmark:api": "node scripts/benchmark.js --api",
    "benchmark:webhook": "node scripts/benchmark.js --webhook",
    "build-info:generate": "node scripts/generate-build-info.js",
    "build-info:check": "node scripts/check-version-drift.js"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "eslint": "^8.0.0",
    "husky": "^9.1.7",
    "jest": "^29.0.0",
    "ts-node": "^10.9.2",
    "yaml": "^2.9.0"
  }
}
